{
  "name": "md-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "now-build": "npm run build && ls && mv client/build dist && ls",
    "server": "nodemon index.js",
    "client": "npm run start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client",
    "generate": "plop"
  },
  "author": "Meszaros Dezso",
  "license": "ISC",
  "dependencies": {
    "@now/node-server": "^0.8.2",
    "axios": "^0.19.0",
    "concurrently": "^5.0.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.8.11",
    "nodemon": "^1.19.4",
    "plop": "^2.5.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "dotenv": "^8.2.0"
  }
}
